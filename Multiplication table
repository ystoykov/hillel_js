var digit1 = 1;
var digit2 = 1;

while (digit1 <= 10) {
    while(digit2 <= 10) {
        console.log(digit1 + " * " + digit2 + " = " + (digit1 * digit2) + "<br />");
        digit2++;
    };
    console.log("<br />");
    digit2 = 1;
    digit1++;
};
